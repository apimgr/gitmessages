# Features & Client Rules (PART 29-32, general)

⚠️ **These rules are NON-NEGOTIABLE. Violations are bugs.** ⚠️

## CRITICAL - NEVER DO
- Never skip the client scope requirement — `src/client/` is expected for all projects
- Never implement a feature not described in IDEA.md just because AI.md's
  general template mentions it

## CRITICAL - ALWAYS DO
- Maintain ReadTheDocs/MkDocs-style `docs/` (index.md, installation.md,
  configuration.md, api.md, cli.md, security.md, integrations.md,
  development.md, stylesheets/, requirements.txt)
- Provide `src/client/` for the CLI client binary (`gitmessages-cli` per IDEA.md)

## Summary
`src/client/` and `docs/` are both currently missing from this project — real
gaps to track in TODO.AI.md, not stub-filled here.

For complete details, see AI.md PART 29-32.
